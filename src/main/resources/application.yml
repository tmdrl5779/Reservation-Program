spring:

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
    initialization-mode: always

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    defer-datasource-initialization: true

logging:
  level:
    com.example: DEBUG


jwt:
  header: Authorization
  #echo 'tmdrl5779-reservateion-program-api'|base64
  secret: dG1kcmw1Nzc5LXJlc2VydmF0ZWlvbi1wcm9ncmFtLWFwaQ==
  token-validity-in-seconds: 86400 #만료시간